
# Day 30 – Remote Server Simulation

Created second user to simulate remote server:
sudo adduser cloudserver

SSH into server:
ssh cloudserver@localhost

Verify remote session:
whoami

Remote files are isolated from local user

exit → returns to local machine

This simulates connecting to a cloud server
